package com.example.javafxtest;

// Wrapper main class — the module system requires a non-Application entry point.
public class Launcher {
    public static void main(String[] args) {
        QuaxApplication.main(args);
    }
}